/**
 * Vertical rhythm for /landing-page.
 *
 * Tighter than the site's `.section` (64px mobile / 112px desktop) on purpose.
 * This page is read on a phone, in one pass, by someone who arrived from an
 * ad and is deciding whether to pay — every extra screen of scroll between the
 * hero and the booking form is a place to leave. One constant so the whole
 * page keeps one rhythm and a change is a single edit.
 */
export const LP_SECTION = "py-14 md:py-20";
